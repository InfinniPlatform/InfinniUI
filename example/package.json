{
  "name": "infinni-ui-example",
  "dependencies": {
    "infinni-ui": "^3.0.0"
  },
  "devDependencies": {
    "browser-sync": "^2.12.x",
    "connect-history-api-fallback": "^1.3.0",
    "del": "^2.2.0",
    "gulp": "github:gulpjs/gulp#4.0",
    "gulp-autoprefixer": "^3.1.0",
    "gulp-cli": "^1.2.2",
    "gulp-concat": "^2.6.0",
    "gulp-csso": "^2.0.0",
    "gulp-if": "^2.0.1",
    "gulp-less": "^3.1.0",
    "gulp-load-plugins": "^1.2.x",
    "gulp-rename": "^1.2.2",
    "gulp-replace": "^0.5.4",
    "gulp-template-compile": "^1.2.0",
    "gulp-uglify": "^1.5.3",
    "gulp-util": "^3.0.8",
    "gulp-watch": "^4.x",
    "gulp-wrapper": "^1.0.0"
  },
  "scripts": {
    "start": "gulp example",
    "build": "npm i && gulp clean && gulp build",
    "build-prod": "npm i && gulp clean && gulp build --production"
  },
  "engines": {
    "node": "4.x",
    "npm": "3.8.x"
  }
}
